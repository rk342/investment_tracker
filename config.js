// Local runtime config for the investment tracker app.
// Set to false to disable Google sign-in and unlock the app locally.
const ENABLE_GOOGLE_SIGN_IN = true;

// Set to false to stop automatically triggering AI analysis when a new ticker is added.
const AUTO_ANALYZE_ON_ADD_TICKER = false;
